import React from 'react';

const EndPage = props => {
    const {
        strings,
    } = props;
    return (
        <div>
            
        </div>
    );
};

export default React.memo(EndPage);